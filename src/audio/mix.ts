// Master mix, one knob per bus. Individual play() calls and music tracks carry
// only their *relative* balance; these constants scale the whole bus.
export const MUSIC_VOLUME = 0.12;
export const SFX_VOLUME = 1;
